# Copy this file to .streamlit/secrets.toml for local testing (that file is
# gitignored and never committed). On Streamlit Community Cloud, paste these
# same keys/values into the app's Settings -> Secrets box instead.

SMTP_EMAIL = "dave24188@gmail.com"
SMTP_APP_PASSWORD = "your-16-character-gmail-app-password"
NOTIFY_EMAIL = "dave24188@gmail.com"
